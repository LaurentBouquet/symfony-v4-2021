{% extends 'base.html.twig' %}

{% block title %}Hello TableController!
{% endblock %}

{% block body %}
	<style>
		.example-wrapper {
			margin: 1em auto;
			max-width: 800px;
			width: 95%;
			font: 18px / 1.5 sans-serif;
		}
		.example-wrapper code {
			background: #F5F5F5;
			padding: 2px 6px;
		}
	</style>

	<div class="example-wrapper" style="color: {{ color }};">
		<h1>Table de {{ calc_name }} par {{ n }} ({{ 1 }} à {{ m }})</h1>

		<table>
			{% for calculation in calculations %}
			<tr>
				<td>{{ calculation.indice }}</td>
				<td> x </td>
				<td>{{ n }}</td>
				<td> = </td>
				<td>{{ calculation.result }}</td>
			</tr>
			{% endfor %}
		</table>


	</div>
{% endblock %}
